[project]
name = "robyn-test"
version = "0.1.0"
description = "Quick Robyn test app"
requires-python = ">=3.12"
dependencies = [
    "robyn~=0.82.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
